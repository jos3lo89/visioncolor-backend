services:
  api:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      # Si decides usar Cloudinary, puedes eliminar esta línea.
      - ./uploads:/app/uploads
      - ./app:/app/app
